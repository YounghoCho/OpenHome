<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--  
 * Application java
 * @Author : Youngho Jo
 *
 * [API 호출 트래픽 Level 5 Steps]
 * Level 1 : 게시글 목록 호출 (article_list)
 * Level 2 : 게시글 내용 호출 (article_detail)
 * Level 3 : 게시글 쓰기 호출 (article_write)
 * Level 4 : 파일 업로드 호출 (file_upload)
 * Level 5 : 파일 다운로드 호출 (file_download)
 -->	
<mapper namespace="apiCall">
	
	<!-- 중복을 방지하기위해 api_call(#{여기에다가 쓰고싶지만 'article_list'처럼 들어와서 에러가난다.}) -->
	<insert id="insertApiCallLevel1" parameterType="com.worksmobile.openhome.model.ApiCall">
	 	INSERT
	 	INTO
	 		api_call(article_list, article_detail, article_write, file_upload, file_download, api_date)
		VALUES
			(1, 0, 0, 0, 0, now())
	</insert>
	<insert id="insertApiCallLevel2" parameterType="com.worksmobile.openhome.model.ApiCall">
	 	INSERT
	 	INTO
	 		api_call(article_list, article_detail, article_write, file_upload, file_download, api_date)
		VALUES
			(0, 1, 0, 0, 0, now())
	</insert>	
	<insert id="insertApiCallLevel3" parameterType="com.worksmobile.openhome.model.ApiCall">
	 	INSERT
	 	INTO
	 		api_call(article_list, article_detail, article_write, file_upload, file_download, api_date)
		VALUES
			(0, 0, 1, 0, 0, now())
	</insert>
	<insert id="insertApiCallLevel4" parameterType="com.worksmobile.openhome.model.ApiCall">
	 	INSERT
	 	INTO
	 		api_call(article_list, article_detail, article_write, file_upload, file_download, api_date)
		VALUES
			(0, 0, 0, 1, 0, now())
	</insert>
	<insert id="insertApiCallLevel5" parameterType="com.worksmobile.openhome.model.ApiCall">
	 	INSERT
	 	INTO
	 		api_call(article_list, article_detail, article_write, file_upload, file_download, api_date)
		VALUES
			(0, 0, 0, 0, 1, now())
	</insert>		
</mapper>